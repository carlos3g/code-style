/**
 * Shared commitlint config — carlos3g's code style.
 *
 * Conventional Commits, English, imperative, lowercase. The rules themselves
 * come from @commitlint/config-conventional; this package exists so every
 * project references one versioned source instead of copying the config.
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
