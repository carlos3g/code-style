{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "check": {
      "inputs": ["src/**", "*.json"]
    },
    "lint": {
      "inputs": ["src/**", "eslint.config.mjs"]
    },
    "test": {
      "cache": false
    }
  }
}
